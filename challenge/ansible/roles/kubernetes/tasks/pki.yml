# See: https://kubernetes.io/docs/setup/best-practices/certificates/

- name: set kubernetes certificates
  set_fact:
    kubernetes_certificates:
      - { name: kube-apiserver, extended_key_usage: ["serverAuth"] }
      - { name: apiserver-kubelet-client, extended_key_usage: ["clientAuth"], subject: {O: "system:masters"}}
      - { name: sa, extended_key_usage: ["clientAuth"] }
      - { name: admin, extended_key_usage: ["clientAuth"] }
      - { name: kube-controller-manager, extended_key_usage: ["clientAuth"] }
      - { name: kube-scheduler, extended_key_usage: ["clientAuth"] }
    subject_alt_name:
      - "DNS:{{ ansible_fqdn }}"
      - "DNS:{{ ansible_hostname }}"
      - "DNS:localhost"
      - "IP:{{ ansible_default_ipv6.address }}"
      - "IP:{{ ansible_default_ipv4.address }}"
      - "IP:{{ pod_cidr_block | regex_replace('[0-9]/[0-9][0-9]?', '1') }}"
      - "IP:127.0.0.1"
      - "IP:::1"
      - "DNS:kubernetes"
      - "DNS:kubernetes.default"
      - "DNS:kubernetes.default.svc"
      - "DNS:kubernetes.default.svc.cluster"
      - "DNS:kubernetes.default.svc.cluster.local"

- name: create key for kubernetes certificates
  openssl_privatekey:
    path: /etc/etcd/{{ item.name }}.key
    size: 2048
    type: RSA
  loop: "{{ kubernetes_certificates }}"

- name: create csr for kubernetes certificates
  openssl_csr:
    path: /etc/etcd/{{ item.name }}.csr
    privatekey_path: /etc/etcd/{{ item.name }}.key
    common_name: "{{ ansible_fqdn }}"
    subject: "{{ item.subject | default(omit) }}"
    subject_alt_name: "{{ subject_alt_name if 'serverAuth' in item.extended_key_usage else omit }}"
    key_usage:
      - digitalSignature
      - keyAgreement
    extended_key_usage: "{{ item.extended_key_usage }}"
  loop: "{{ kubernetes_certificates }}"

- name: sign kubernetes certificates
  openssl_certificate:
    path: /etc/etcd/{{ item.name }}.crt
    csr_path: /etc/etcd/{{ item.name }}.csr
    ownca_path: /etc/etcd/ca.crt
    ownca_privatekey_path: /etc/etcd/ca.key
    ownca_not_after: "+3650d"
    provider: ownca
  loop: "{{ kubernetes_certificates }}"

