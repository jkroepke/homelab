storage:
  files:
    - path: "${path}"
      filesystem: rootfs
      mode: ${mode}
      user:
        name: ${user}
      group:
        name: ${group}
      contents:
        inline: |
          ${indent(10, content)}
